<header class="navbar navbar-fixed-top navbar-inverse">
  <div class="container">
    <%= link_to t("title"), root_path, id: "logo" %>
    <nav>
      <ul class="nav navbar-nav navbar-right">
        <li><%= link_to t("help"), help_path %></li>
        <% if user_signed_in? %>
          <% if current_user.admin? %>
            <li class="dropdown">
              <%= link_to t("all_categories"), "#", class: "dropdown-toggle",
                data: {toggle: "dropdown"} %>
              <ul class="dropdown-menu">
                <li><%= link_to t("all_categories"), admin_categories_path %></li>
                <li><%= link_to t("Create"), new_admin_category_path %></li>
              </ul>
            </li>
            <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                <%= current_user.name %> <b class="caret"></b>
              </a>
              <ul class="dropdown-menu">
                <li><%= link_to t("settings"), edit_user_registration_path %></li>
                <li class="divider"></li>
                <li>
                  <%= link_to t("sign_out"), destroy_user_session_path, method: "delete" %>
                </li>
              </ul>
            </li>
          <% else %>
            <li>
              <%= link_to t(:exams), exams_path %>
            </li>
            <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                <%= current_user.name %> <b class="caret"></b>
              </a>
              <ul class="dropdown-menu">
                <li><%= link_to t("settings"), edit_user_registration_path %></li>
                <li class="divider"></li>
                <li>
                  <%= link_to t("sign_out"), destroy_user_session_path, method: "delete" %>
                </li>
              </ul>
            </li>
          <% end %>
        <% else %>
          <li><%= link_to t("sign_in"), new_user_session_path %></li>
        <% end %>
      </ul>
    </nav>
  </div>
</header>
